management:
 endpoint:
   info:
     enabled: true  
   health:
     enabled: true
  shutdown:
    enabled: true
  endpoints:
    web:
      exposure:
        include: refresh, status, info 
server:
  port: 9191
